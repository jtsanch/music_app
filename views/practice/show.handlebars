<script>
	var critiquer_id = {{critiquer_id}};
	var musician_id  = {{musician_id}};
	var started = {{is_recording}}

	function toggle(button) {
     	switch(button.value) {
          case "Start rehearsal":
               button.value = "Stop rehearsal";
               //update fb, start recording and switch critiquer's interface
               break;
          case "Stop rehearsal":
          	   //update fb is_recording value, start recording and switch critiquer's interface
               button.value = "Start rehearsal";
               break;
     	}
	}
</script>
<script src='/javascripts/practice_session.js'></script>
{{#if if_musician}}
	<div class="pure-u-2-3" id="video-container">
	    <video class="outer" id="my-video" muted="true" autoplay=""></video>
	    <video class="inner" id="their-video" autoplay=""></video>
	    
	    <div id="start-button">
    		<input type="button" id="start" value="Start rehearsal" class="btn btn-default btn-lg" onclick="toggle(this);">
		</div>

	</div>
{{else}}
	<div class="pure-u-2-3" id="video-container">
	    <video class="inner"id="my-video" muted="true" autoplay=""></video>
	    <video class="outer" id="their-video" autoplay=""></video>
	</div>
	{{#is recording}}
		<div class="well button-panel">
	        <h3>Click tags to comment on performance</h3>
	        <ul class="tags">
	            <div class="tag-pair btn-group btn-group-lg">
	                <button type="button" class="btn btn-default up" id="good-pitch" value="good pitch">Pitch</button>
	                <button type="button" class="btn btn-default down" id="bad-pitch" value="bad pitch">Pitch</button>
	            </div>
	            <div class="tag-pair btn-group btn-group-lg">
	                <button type="button" class="btn btn-default up" id="good-tone" value="good tone">Tone</button>
	                <button type="button" class="btn btn-default down" id="bad-tone" value="bad pitch">Tone</button>
	            </div>
	            <div class="tag-pair btn-group btn-group-lg">
	                <button type="button" class="btn btn-default up" id="good-tone" value="good rhythm">Rhythm</button>
	                <button type="button" class="btn btn-default down" id="bad-tone" value="bad pitch">Rhythm</button>
	            </div>
	            <div class="tag-pair btn-group btn-group-lg">
	                <button type="button" class="btn btn-default up" id="good-posture" value="good posture">Posture</button>
	                <button type="button" class="btn btn-default down" id="bad-posture" value="bad posture">Posture</button>
	            </div>
	        </ul>
    	</div>
	{{/if}}
{{/if}}